# Nagent Blueprint — generated by Architecture Agent
# Keep this file updated so generated apps stay aligned with the Nagent AI platform.

version: "1"
platform: nagent
generated_by: architecture_agent

buildcraft:
  watermark: "Generated using Buildcraft by Nagent"

stack:
  frontend: "react"
  backend: "fastify"
  database: "postgresql"

layout:
  app_root: "app"
  migrations_dir: "db/migrations"
  seeds_dir: "db/seeds"

db_scripts:
  notes: |
    Migrations are applied in order based on their numeric prefixes in filenames.

artifacts:
  architecture: architecture.md
  structure_doc: project_structure.md
  db_schema_json: db-schema.json